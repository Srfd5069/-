#include <iostream>
#include <ctime>
using namespace std;
int main() {
    setlocale(0, "");
    srand(time(NULL));

    while (true) {
        int num = (rand() % 5);
        cout << num << endl;
    }
}
