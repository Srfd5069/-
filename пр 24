#include <iostream>
#include <Windows.h>
using namespace std;
int main() {
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	const int SIZE = 7;
	int array[SIZE]{};
	int action;
	int max_position[SIZE]{};
	int min_position[SIZE]{};
	int mat[3]{};
	int asd;
	bool cycle = true;

	while (true) {
		system("cls");
		for (int i = 0; i < SIZE; i++) {
			cout << "Инициализируйте ячейку номер " << i + 1 << ":";
			cin >> array[i];
		} 
		cycle = true;
		while (cycle) {
			system("cls");
			cout << "Настройки массива: " << endl << endl << "1.Сортировка по возрастанию" << endl << "2.Сортировка по убыванию" << endl << "3.Перемножить массив" << endl << "4.Сложить массив" << endl << "5.Разделить массив" << endl << "6.Обнулить массив" << endl << endl << "9.Задать значение массиву заново" << endl;
			cin >> action;
			switch (action) {
			case 1: {
				system("cls");
				for (int i = 0; i < SIZE; i++) {
					for (int j = 0; j < SIZE - 1; j++) {
						if (array[j] > array[j + 1]) {
							int buff = array[j];

							array[j] = array[j + 1];
							array[j + 1] = buff;
						} 
					} 
				} 
				cout << "1.Сортировка по возрастанию" << endl;
				for (int i = 0; i < SIZE; i++) {
					cout << array[i] << endl;
				} 
				cout << "Что бы вернутся в меню введите любое число" << endl;
				cin >> asd;
				break;
			} 
			case 2: {
				system("cls");
				for (int i = 0; i < SIZE; i++) {
					for (int j = 0; j < SIZE - 1; j++) {
						if (array[j] < array[j + 1]) {
							int buff = array[j];

							array[j] = array[j + 1];
							array[j + 1] = buff;
						} 
					} 
				} 

				cout << "2.Сортировка по убыванию" << endl;
				for (int i = 0; i < SIZE; i++) {
					cout << array[i] << endl;
				} 
				cout << "Что бы вернутся в меню введите любое число" << endl;
				cin >> asd;
				break;
			} 
			case 3: {
				system("cls");
				cout << "Введите число для умножения: ";
				cin >> mat[0];
				system("cls");
				cout << "3.Перемножить массив" << endl;
				for (int i = 0; i < SIZE; i++) {
					cout << array[i] * mat[0] << endl;
				} 
				cout << "Что бы вернутся в меню введите любое число" << endl;
				cin >> asd;
				break;
			} 
			case 4: {
				system("cls");
				cout << "Введите число для сложения: ";
				cin >> mat[1];
				system("cls");
				cout << "4.Сложить массив" << endl;
				for (int i = 0; i < SIZE; i++) {
					cout << array[i] + mat[1] << endl;
				} 
				cout << "Что бы вернутся в меню введите любое число" << endl;
				cin >> asd;
				break;
			} 
			case 5: {
				system("cls");
				cout << "Введите число для деления: ";
				cin >> mat[2];
				system("cls");
				cout << "5.Разделить массив" << endl;
				for (int i = 0; i < SIZE; i++) {
					cout << array[i] / mat[2] << endl;
				} 
				cout << "Что бы вернутся в меню введите любое число" << endl;
				cin >> asd;
				break;
			} 
			case 6: {
				system("cls");
				for (int i = 0; i < SIZE; i++) {
					array[i] = 0;
				} 
				cout << "Массив обнулен" << endl;
				cin >> asd;
				break;
			} 
			case 9: {
				cycle = false;
				break;
			}
			} 
		} 
	} 
}
