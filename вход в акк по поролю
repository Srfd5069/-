#include <iostream>;
using namespace std;

int main() {
	setlocale(0, "");

	cout << "Введите пароль:";
	string password;
	cin >> password;
	if (password == "123a") {
		cout << "Доступ разрешён";
	}else {
		cout << "Доступ запрещён"
	}
}
